// DEPRECATED: CostCenter feature removed in favor of boolean `planned` field (f006 refactor).
// File kept to avoid deletion issues.

import { describe, it } from 'vitest';

describe.skip('CostCenterService', () => {
  it('is deprecated', () => {
    // All cost-center tests removed; feature no longer in use.
  });
});
