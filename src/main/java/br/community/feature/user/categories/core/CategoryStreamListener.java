package br.community.feature.user.categories.core;

import org.jspecify.annotations.NullMarked;

/** Placeholder — category SSE events are now dispatched directly from the feature service layer. */
@NullMarked
public class CategoryStreamListener {
    // Category events are no longer routed through MessageBus.
}
