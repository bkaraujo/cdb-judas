package br.community.feature.user.tags.core;

import org.jspecify.annotations.NullMarked;

/** Placeholder — tag SSE events are now dispatched directly from the feature service layer. */
@NullMarked
public class TagStreamListener {
    // Tag events are no longer routed through MessageBus.
}
