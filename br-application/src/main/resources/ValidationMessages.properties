# Bundle raiz (inglês — mesmo idioma-raiz nativo do Hibernate Validator para as constraints padrão,
# então esta é a única entrada em inglês que este projeto precisa escrever: as ~34 constraints padrão
# (@NotBlank, @NotNull, @Size...) já vêm em inglês do próprio Hibernate Validator sem override nenhum
# aqui). Só as 2 chaves customizadas do projeto (fora do vocabulário padrão do Jakarta Validation)
# precisam de entrada própria — texto idêntico ao que já existia hardcoded nas anotações.
cdb.validation.twoDecimalPlaces=must have at most 2 decimal places
cdb.validation.closingFormat=Invalid format. Use YYYY-MM
