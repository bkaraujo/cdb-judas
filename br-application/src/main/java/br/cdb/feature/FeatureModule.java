package br.cdb.feature;

import jakarta.inject.Singleton;
import org.jspecify.annotations.NullMarked;

/**
 * Vazio: todo produtor migrou para o {@code FNNNModule} da própria fatia (reestruturação fNNN,
 * .claude/refactor.md). Mantido só até a limpeza final (usuário precisa rodar {@code !rm} — remoção
 * de arquivo é bloqueada nesta sessão); pode ser removido com segurança quando isso acontecer.
 */
@NullMarked
@Singleton
public class FeatureModule {
}
