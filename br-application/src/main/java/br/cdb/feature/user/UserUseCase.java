package br.cdb.feature.user;

import jakarta.inject.Singleton;
import org.jspecify.annotations.NullMarked;

/**
 * Vazio: todo método migrou para o use case da própria fatia (reestruturação fNNN,
 * .claude/refactor.md). Mantido só até a limpeza final (deleção + reescrita da regra ArchUnit 7,
 * que hoje casa {@code feature.user.(*)}) — ver item 7 do plano.
 */
@NullMarked
@Singleton
public class UserUseCase {
}
